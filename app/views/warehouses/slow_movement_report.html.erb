<h1>Slow Movement Report for <%= @warehouse.name %></h1>

<% if @slow_moving_items.present? %>  <!-- Safely checks for nil and empty array -->
  <h2>Slow Moving Items</h2>
  <ul>
    <% @slow_moving_items.each do |item| %>
      <li><%= item.name %> - <%= item.quantity %></li>
    <% end %>
  </ul>
<% else %>
  <p>No slow-moving items found.</p>
<% end %>
